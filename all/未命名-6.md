```mermaid
classDiagram
    DiscussbokewenzhangServiceImpl ..|> IDiscussbokewenzhangService : implements
    DiscussbokewenzhangController ..> IDiscussbokewenzhangService : 依赖
    DiscussbokewenzhangServiceImpl ..> IDiscussbokewenzhangMapper : 依赖
    IDiscussbokewenzhangMapper ..> DiscussbokewenzhangEntity : 关联

    class DiscussbokewenzhangController {
        - IDiscussbokewenzhangService discussbokewenzhangService
        + Result publishComment(DiscussbokewenzhangAddDto dto)
        + Result getCommentByBlogId(Integer blogId, Integer pageNum, Integer pageSize)
        + Result deleteComment(Integer commentId)
        
       
            - Integer blogId
            - String content
            - Integer userId
            - Integer parentId
            + Integer getBlogId()
            + String getContent()
            + Integer getUserId()
            + Integer getParentId()
            + void setContent(String content)
        }
        
        class Result {
            - Integer code
            - String msg
            - Object data
            + static Result success(Object data)
            + static Result error(String msg)
            + static Result success()
            + Integer getCode()
            + String getMsg()
            + Object getData()
        }
    }

    class IDiscussbokewenzhangService {
        + Integer addComment(Integer blogId, String content, Integer userId, Integer parentId)
        + PageResult getCommentByBlogId(Integer blogId, Integer pageNum, Integer pageSize)
        + Boolean deleteComment(Integer commentId)
        + Boolean checkCommentOwner(Integer commentId, Integer userId)
    }

    class DiscussbokewenzhangServiceImpl {
        - IDiscussbokewenzhangMapper discussbokewenzhangMapper
        + Integer addComment(Integer blogId, String content, Integer userId, Integer parentId)
        + PageResult getCommentByBlogId(Integer blogId, Integer pageNum, Integer pageSize)
        + Boolean deleteComment(Integer commentId)
        + Boolean checkCommentOwner(Integer commentId, Integer userId)
    }

    class IDiscussbokewenzhangMapper {
        + Integer insert(DiscussbokewenzhangEntity entity)
        + List selectByBlogId(Integer blogId, Integer start, Integer size)
        + Long countByBlogId(Integer blogId)
        + Integer deleteById(Integer commentId)
        + Integer countByCommentIdAndUserId(Integer commentId, Integer userId)
    }

    class DiscussbokewenzhangEntity {
        - Integer commentId
        - Integer blogId
        - String content
        - Integer userId
        - Integer parentId
        - Integer status
        - Date createTime
        - Date updateTime
        + Integer getCommentId()
        + Integer getBlogId()
        + String getContent()
        + Integer getUserId()
        + Integer getParentId()
        + Integer getStatus()
        + void setBlogId(Integer blogId)
        + void setContent(String content)
        + void setUserId(Integer userId)
        + void setParentId(Integer parentId)
        + void setStatus(Integer status)
    }

    class PageResult {
        - Long total
        - Integer pages
        - List list
        - Integer pageNum
        - Integer pageSize
        + List getList()
        + Long getTotal()
        + Integer getPages()
        + Integer getPageNum()
        + Integer getPageSize()
    }
```