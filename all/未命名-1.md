```mermaid
classDiagram
    %% 核心关系定义
    ArticleServiceImpl ..|> ArticleService  
    ArticleEntity "n" -- "n" TagEntity : 关联 via ArticleTagEntity  
    ArticleTagEntity ..> ArticleEntity     
    ArticleTagEntity ..> TagEntity         
    
    %% 依赖关系
    ArticleController ..> ArticleService
    ArticleController ..> ArticlePublishDto
    ArticleServiceImpl ..> ArticleMapper
    ArticleServiceImpl ..> ArticleTagMapper
    ArticleServiceImpl ..> CategoryMapper
    ArticleService ..> ArticleEntity
    ArticleService ..> ArticlePublishDto
    ArticleMapper ..> ArticleEntity
    ArticleTagMapper ..> ArticleTagEntity

    %% 文章实体类
    class ArticleEntity {
        - Integer articleId
        - String title
        - String content
        - Integer authorId
        - Integer categoryId
        - Integer status
        - Date publishedTime
        - Integer pageView
        - Integer commentCount
        + Integer getArticleId()
        + void setStatus(Integer status)
    }
    %% 文章标签关联实体类（处理多对多关系）
    class ArticleTagEntity {
        - Integer middleId
        - Integer articleId
        - Integer tagId
        + Integer getArticleId()
        + Integer getTagId()
    }

    %% 标签实体类（补充多对多关联的基础类）
    class TagEntity {
        - Integer tagId
        - String tagName
        - Integer status
        + Integer getTagId()
    }

    %% 博客发布请求类
    class ArticlePublishDto {
        - String title
        - String content
        - Integer categoryId
        - List<Integer> tagIds
        - Boolean isDraft
        + String getTitle()
        + String getContent()
    }
    %% 控制器类
    class ArticleController {
        - ArticleService articleService
        + Result publishArticle(ArticlePublishDto dto, Integer userId)
        + Result saveDraft(ArticlePublishDto dto, Integer userId)
        + Result updateArticle(Integer articleId, ArticlePublishDto dto)
    }

    %% 服务接口
    interface ArticleService {
        + Integer publish(ArticleEntity article, List<Integer> tagIds)
        + Integer saveDraft(ArticleEntity article)
        + Boolean updateArticle(ArticleEntity article, List<Integer> tagIds)
    }

    %% 服务实现类
    class ArticleServiceImpl {
        - ArticleMapper articleMapper
        - ArticleTagMapper articleTagMapper
        - CategoryMapper categoryMapper
        + Integer publish(ArticleEntity article, List<Integer> tagIds)
        + Integer saveDraft(ArticleEntity article)
    }

```