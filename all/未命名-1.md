```mermaid
classDiagram
    %% 关系定义
    ArticleAuditServiceImpl ..|> ArticleAuditService : 实现
    ArticleAuditController ..> ArticleAuditService : 依赖
    ArticleAuditController ..> ArticleAuditQueryDto : 依赖
    ArticleAuditController ..> ArticleAuditDto : 依赖
    ArticleAuditServiceImpl ..> ArticleMapper : 依赖
    ArticleAuditServiceImpl ..> MessageService : 依赖
    ArticleAuditServiceImpl ..> OperateLogMapper : 依赖
    AdminEntity "1" -- "n" ArticleEntity : 一对多（审核）
    ArticleAuditDto -- ArticleEntity : 关联（articleId）

    %% 核心类定义
    class ArticleAuditQueryDto {
        - String title
        - String authorName
        - Date submitTimeStart
        - Date submitTimeEnd
        - Integer pageNum
        + String getTitle()
        + Date getSubmitTimeStart()
    }

    class ArticleAuditDto {
        - Integer articleId
        - Integer auditResult
        - String auditReason
        + Integer getArticleId()
        + Integer getAuditResult()
    }

    class ArticleAuditController {
        - ArticleAuditService articleAuditService
        + PageResult<ArticleEntity> getAuditArticleList(ArticleAuditQueryDto dto)
        + Result auditArticle(ArticleAuditDto dto, Integer adminId)
        + Result batchAuditArticle(List<Integer> articleIds, Integer auditResult, String reason)
    }

    interface ArticleAuditService {
        + PageResult<ArticleEntity> getAuditArticleList(ArticleAuditQueryDto dto)
        + Boolean auditArticle(Integer articleId, Integer auditResult, String reason, Integer adminId)
        + Boolean notifyUserAuditResult(Integer articleId, Integer result, String reason)
    }

    class ArticleAuditServiceImpl {
        - ArticleMapper articleMapper
        - MessageService messageService
        - OperateLogMapper operateLogMapper
        + PageResult<ArticleEntity> getAuditArticleList(ArticleAuditQueryDto dto)
        + Boolean auditArticle(Integer articleId, Integer auditResult, String reason, Integer adminId)
    }

    class ArticleEntity {
        - Integer articleId
        - String title
        - Integer status
        - Integer authorId
        + Integer getArticleId()
        + void setStatus(Integer status)
    }

    class MessageService {
        + Boolean sendMessage(MessageEntity message)
    }

    class MessageEntity {
        - Integer userId
        - String content
        + Integer getUserId()
    }

    class OperateLogMapper {
        + Integer insertOperateLog(OperateLogEntity log)
    }

    class PageResult<T> {
        - Long total
        - List<T> list
        + List<T> getList()
    }
```