```mermaid
classDiagram
    %% 核心关系定义
    ArticleServiceImpl ..|> ArticleService
    ArticleController ..> ArticleService
    ArticleController ..> ArticleQueryDto
    ArticleController ..> PageResult
    ArticleService ..> ArticleQueryDto
    ArticleService ..> PageResult
    ArticleService ..> ArticleVo
    ArticleService ..> ArticleEntity
    ArticleVo ..> ArticleEntity            
    ArticleVo ..> UserEntity    
    ArticleVo ..> TagEntity      
    ArticleServiceImpl ..> ArticleMapper
    ArticleServiceImpl ..> ArticleTagMapper
    ArticleServiceImpl ..> CategoryMapper
    ArticleServiceImpl ..> TagMapper
    ArticleServiceImpl ..> UserMapper

    %% 博客列表查询条件类
    class ArticleQueryDto {
        - Integer categoryId
        - Integer tagId
        - String sortType
        - Integer pageNum
        - Integer pageSize
        + Integer getCategoryId()
        + Integer getPageNum()
    }
    %% 博客列表分页结果类（泛型）
    class PageResult {
        - Long total
        - Integer pages
        - List<T> list
        - Integer pageNum
        - Integer pageSize
        + List<T> getList()
        + Long getTotal()
    }
    %% 博客详情VO类（返回前端展示数据）
    class ArticleVo {
        - Integer articleId
        - String title
        - String content
        - String authorName
        - String authorAvatar
        - String categoryName
        - List<String> tagNames
        - Date publishedTime
        - Integer pageView
        - Integer commentCount
        + Integer getArticleId()
    }

    %% 控制器类（整合所有文章相关接口）
    class ArticleController {
        - ArticleService articleService
        + Result publishArticle(ArticlePublishDto dto, Integer userId)
        + Result saveDraft(ArticlePublishDto dto, Integer userId)
        + Result updateArticle(Integer articleId, ArticlePublishDto dto)
        + PageResult<ArticleVo> getArticleList(ArticleQueryDto queryDto)
        + ArticleVo getArticleDetail(Integer articleId)
    }

    %% 服务接口（整合所有文章相关业务规范）
    class ArticleService {
        + Integer publish(ArticleEntity article, List<Integer> tagIds)
        + Integer saveDraft(ArticleEntity article)
        + Boolean updateArticle(ArticleEntity article, List<Integer> tagIds)
        + PageResult<ArticleEntity> queryArticleList(ArticleQueryDto queryDto)
        + ArticleEntity getArticleDetail(Integer articleId)
        + ArticleVo convertToVo(ArticleEntity article)
    }


```