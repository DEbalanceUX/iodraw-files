```mermaid
classDiagram
    %% 关系定义
    FeedbackServiceImpl ..|> FeedbackService
    UserEntity "1" -- "n" FeedbackEntity
    FeedbackController ..> FeedbackService
    FeedbackController ..> FeedbackDto
    FeedbackController ..> FeedbackVo
    FeedbackService ..> FeedbackEntity
    FeedbackServiceImpl ..> FeedbackMapper
    FeedbackMapper ..> FeedbackEntity
    FeedbackVo ..> FeedbackEntity

    %% 核心类定义
    class FeedbackEntity {
        - Integer feedbackId
        - Integer userId
        - Integer feedbackType
        - String content
        - String screenshot
        - Date feedbackTime
        - Integer status
        - String replyContent
        + Integer getFeedbackId()
        + String getContent()
    }

    class FeedbackDto {
        - Integer feedbackType
        - String content
        - String screenshot
        + Integer getFeedbackType()
        + String getContent()
    }

    class FeedbackVo {
        - Integer feedbackId
        - Integer feedbackType
        - String content
        - Date feedbackTime
        - Integer status
        - String replyContent
        + Integer getFeedbackId()
    }

    class FeedbackController {
        - FeedbackService feedbackService
        + Result submitFeedback(FeedbackDto dto, Integer userId)
        + List<FeedbackVo> getMyFeedbacks(Integer userId)
    }

    interface FeedbackService {
        + Integer submitFeedback(FeedbackEntity feedback)
        + List<FeedbackEntity> getFeedbacksByUserId(Integer userId)
        + FeedbackVo convertToVo(FeedbackEntity feedback)
    }

    class FeedbackServiceImpl {
        - FeedbackMapper feedbackMapper
        + Integer submitFeedback(FeedbackEntity feedback)
        + List<FeedbackEntity> getFeedbacksByUserId(Integer userId)
        + FeedbackVo convertToVo(FeedbackEntity feedback)
    }

    class FeedbackMapper {
        + Integer insertFeedback(FeedbackEntity feedback)
        + List<FeedbackEntity> selectByUserId(Integer userId)
    }

    class UserEntity {
        - Integer userId
        + Integer getUserId()
    }
```