```mermaid
classDiagram
    %% 关系定义
    UserManageServiceImpl ..|> UserManageService : 实现
    UserManageController ..> UserManageService : 依赖
    UserManageController ..> UserManageQueryDto : 依赖
    UserManageController ..> UserOperateDto : 依赖
    UserManageController ..> PageResult : 依赖
    UserManageServiceImpl ..> UserMapper : 依赖
    UserManageServiceImpl ..> OperateLogMapper : 依赖
    OperateLogEntity "n" -- "1" AdminEntity : 多对一
    OperateLogEntity "n" -- "1" UserEntity : 多对一

    %% 核心类定义
    class UserManageQueryDto {
        - String username
        - Integer userId
        - Integer status
        - Integer pageNum
        - Integer pageSize
        + String getUsername()
        + Integer getPageNum()
    }

    class UserOperateDto {
        - Integer userId
        - Integer operateType
        - String operateReason
        + Integer getUserId()
        + Integer getOperateType()
    }

    class UserManageController {
        - UserManageService userManageService
        + PageResult<UserEntity> getUserList(UserManageQueryDto dto)
        + Result operateUser(UserOperateDto dto, Integer adminId)
        + Result batchOperateUser(List<Integer> userIds, Integer operateType)
    }

    class UserManageService {
        + PageResult<UserEntity> queryUserList(UserManageQueryDto dto)
        + Boolean operateUser(Integer userId, Integer operateType, String reason, Integer adminId)
        + Boolean batchOperateUser(List<Integer> userIds, Integer operateType, String reason)
    }

    class UserManageServiceImpl {
        - UserMapper userMapper
        - OperateLogMapper operateLogMapper
        + PageResult<UserEntity> queryUserList(UserManageQueryDto dto)
        + Boolean operateUser(Integer userId, Integer operateType, String reason, Integer adminId)
    }

    class OperateLogEntity {
        - Integer logId
        - Integer adminId
        - String operateType
        - Integer targetId
        - String operateContent
        - Date operateTime
        + Integer getLogId()
    }

    class PageResult<T> {
        - Long total
        - Integer pages
        - List<T> list
        - Integer pageNum
        + List<T> getList()
        + Long getTotal()
    }

    class OperateLogMapper {
        + Integer insertOperateLog(OperateLogEntity log)
    }

    class UserMapper {
        + List<UserEntity> selectUserByCondition(UserManageQueryDto dto)
        + Integer updateUserStatus(Integer userId, Integer status)
        + Long countUserByCondition(UserManageQueryDto dto)
    }

    class UserEntity {
        - Integer userId
        - String username
        - Integer status
        + Integer getUserId()
    }

    class AdminEntity {
        - Integer userId
        + Integer getUserId()
    }
```