```mermaid
classDiagram
    %% 关系定义
    CategoryServiceImpl ..|> CategoryService : 实现
    TagServiceImpl ..|> TagService : 实现
    CategoryTagController ..> CategoryService : 依赖
    CategoryTagController ..> TagService : 依赖
    CategoryTagController ..> CategoryManageDto : 依赖
    CategoryTagController ..> TagManageDto 
    CategoryEntity "n" -- "1" CategoryEntity : 多对一（父分类）
    CategoryEntity "1" -- "n" ArticleEntity : 一对多

    %% 核心类定义
    class CategoryManageDto {
        - Integer categoryId
        - String categoryName
        - Integer parentId
        - Integer sort
        + Integer getCategoryId()
        + Integer getParentId()
    }

    class TagManageDto {
        - Integer tagId
        - String tagName
        - Boolean isHot
        + Integer getTagId()
        + Boolean getIsHot()
    }

    class CategoryTagController {
        - CategoryService categoryService
        - TagService tagService
        + Result addCategory(CategoryManageDto dto)
        + Result updateCategory(CategoryManageDto dto)
        + Result deleteCategory(Integer categoryId)
        + Result addTag(TagManageDto dto)
        + Result updateTagStatus(Integer tagId, Boolean isHot)
    }

    interface CategoryService {
        + Boolean addCategory(CategoryEntity category)
        + Boolean updateCategory(CategoryEntity category)
        + Boolean deleteCategory(Integer categoryId)
        + Boolean checkCategoryName(String name)
    }

    class CategoryServiceImpl {
        - CategoryMapper categoryMapper
        + Boolean addCategory(CategoryEntity category)
        + Boolean deleteCategory(Integer categoryId)
    }

    interface TagService {
        + Boolean addTag(TagEntity tag)
        + Boolean updateTag(TagEntity tag)
        + Boolean deleteTag(Integer tagId)
    }

    class TagServiceImpl {
        - TagMapper tagMapper
        + Boolean addTag(TagEntity tag)
    }

    class CategoryEntity {
        - Integer categoryId
        - String categoryName
        - Integer parentId
        - Integer sort
        + Integer getCategoryId()
    }

    class TagEntity {
        - Integer tagId
        - String tagName
        - Boolean isHot
        + Integer getTagId()
    }

    class ArticleEntity {
        - Integer articleId
        - Integer categoryId
        + Integer getArticleId()
    }

    interface CategoryMapper {
        + Integer insertCategory(CategoryEntity category)
        + Integer updateCategory(CategoryEntity category)
        + Long countArticleByCategory(Integer categoryId)
    }

    interface TagMapper {
        + Integer insertTag(TagEntity tag)
        + Integer updateTag(TagEntity tag)
    }
```