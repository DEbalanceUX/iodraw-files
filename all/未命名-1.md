```mermaid
classDiagram
    %% 关系定义
    ArticleServiceImpl ..|> ArticleService
    ArticleController ..> ArticleService
    ArticleController ..> DraftQueryDto
    ArticleController ..> PageResult
    ArticleServiceImpl ..> ArticleMapper
    ArticleService ..> ArticleEntity
    ArticleEntity -- DraftQueryDto : 关联（status字段）

    %% 核心类定义
    class DraftQueryDto {
        - Integer userId
        - Integer pageNum
        - Integer pageSize
        + Integer getUserId()
        + Integer getPageNum()
    }

    class ArticleController {
        - ArticleService articleService
        + PageResult<ArticleVo> getDraftList(DraftQueryDto dto)
        + Result deleteDraft(Integer articleId, Integer userId)
        + Result editDraft(Integer articleId, ArticlePublishDto dto)
    }

    interface ArticleService {
        + PageResult<ArticleEntity> getDraftList(Integer userId, Integer pageNum, Integer pageSize)
        + Boolean deleteDraft(Integer articleId, Integer userId)
        + Boolean updateDraft(ArticleEntity article, List<Integer> tagIds)
    }

    class ArticleServiceImpl {
        - ArticleMapper articleMapper
        - articleTagMapper: ArticleTagMapper
        + PageResult<ArticleEntity> getDraftList(Integer userId, Integer pageNum, Integer pageSize)
        + Boolean deleteDraft(Integer articleId, Integer userId)
    }

    class ArticleMapper {
        + List<ArticleEntity> selectDraftsByUserId(Integer userId, Integer start, Integer size)
        + Long countDraftsByUserId(Integer userId)
        + Integer deleteArticle(Integer articleId, Integer userId)
    }

    class ArticleEntity {
        - Integer articleId
        - Integer status
        - Integer authorId
        + Integer getArticleId()
        + void setStatus(Integer status)
    }

    class ArticlePublishDto {
        - String title
        - String content
        - List<Integer> tagIds
        + String getTitle()
    }

    class PageResult<T> {
        - Long total
        - List<T> list
        + List<T> getList()
        + Long getTotal()
    }

    class ArticleVo {
        - Integer articleId
        - String title
        + Integer getArticleId()
    }
```