```mermaid
classDiagram
    %% 关系定义
    AdminEntity --|> UserEntity : 继承
    AdminServiceImpl ..|> AdminService : 实现
    AdminController ..> AdminService : 依赖
    AdminController ..> AdminLoginDto : 依赖
    AdminServiceImpl ..> AdminMapper : 依赖
    AdminServiceImpl ..> VerifyCodeService : 依赖
    AdminService ..> AdminEntity : 关联
    AdminMapper ..> AdminEntity : 关联

    %% 核心类定义
    class UserEntity {
        - Integer userId
        - String username
        - String password
        - String nickname
        - String avatar
        - Integer status
        - Date createTime
        + Integer getUserId()
        + void setPassword(String password)
    }

    class AdminEntity {
        - Integer roleLevel
        - String lastLoginIp
        - Date lastLoginTime
        + Integer getRoleLevel()
        + void setLastLoginInfo(String ip, Date time)
    }

    class AdminLoginDto {
        - String username
        - String password
        - String verifyCode
        + String getUsername()
        + String getVerifyCode()
    }

    class AdminController {
        - AdminService adminService
        - UserManageService userManageService
        - ArticleAuditService articleAuditService
        + Result adminLogin(AdminLoginDto dto)
        + AdminVo getAdminInfo(Integer adminId)
    }

    interface AdminService {
        + AdminEntity login(String username, String password, String verifyCode)
        + Boolean checkAdminRole(Integer adminId, Integer roleLevel)
    }

    class AdminServiceImpl {
        - AdminMapper adminMapper
        - VerifyCodeService verifyCodeService
        + AdminEntity login(String username, String password, String verifyCode)
        + Boolean checkAdminRole(Integer adminId, Integer roleLevel)
    }

    interface AdminMapper {
        + AdminEntity selectByUsername(String username)
        + Integer updateLoginInfo(Integer adminId, String ip, Date time)
    }

    class AdminVo {
        - Integer userId
        - String username
        - Integer roleLevel
        - String lastLoginIp
        + Integer getUserId()
    }

    class VerifyCodeService {
        + Boolean checkVerifyCode(String code, String key)
    }
```