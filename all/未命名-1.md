```mermaid
classDiagram
    %% 关系定义
    FeedbackReplyServiceImpl ..|> FeedbackReplyService : 实现
    CommentManageServiceImpl ..|> CommentManageService : 实现
    InteractionManageController ..> CommentManageService : 依赖
    InteractionManageController ..> FeedbackReplyService : 依赖
    InteractionManageController ..> CommentManageDto : 依赖
    InteractionManageController ..> FeedbackReplyDto : 依赖
    FeedbackReplyServiceImpl ..> FeedbackMapper : 依赖
    FeedbackReplyServiceImpl ..> MessageService : 依赖
    FeedbackEntity "n" -- "1" AdminEntity : 多对一
    CommentManageDto -- CommentEntity : 关联（commentId）

    %% 核心类定义
    class CommentManageDto {
        - Integer commentId
        - Integer handleType
        - String handleReason
        + Integer getCommentId()
        + Integer getHandleType()
    }

    class FeedbackReplyDto {
        - Integer feedbackId
        - String replyContent
        - Integer handleStatus
        + Integer getFeedbackId()
        + String getReplyContent()
    }

    class InteractionManageController {
        - CommentManageService commentManageService
        - FeedbackReplyService feedbackReplyService
        + Result handleComment(CommentManageDto dto)
        + Result batchHandleComment(List<Integer> commentIds, Integer handleType)
        + Result replyFeedback(FeedbackReplyDto dto, Integer adminId)
        + PageResult<FeedbackEntity> getFeedbackList(Integer status)
    }

    interface CommentManageService {
        + Boolean handleComment(Integer commentId, Integer handleType, String reason)
        + Boolean batchHandleComment(List<Integer> commentIds, Integer handleType)
    }

    class CommentManageServiceImpl {
        - CommentMapper commentMapper
        + Boolean handleComment(Integer commentId, Integer handleType, String reason)
    }

    interface FeedbackReplyService {
        + Boolean replyFeedback(Integer feedbackId, String replyContent, Integer adminId)
        + Boolean updateFeedbackStatus(Integer feedbackId, Integer status)
    }

    class FeedbackReplyServiceImpl {
        - FeedbackMapper feedbackMapper
        - MessageService messageService
        + Boolean replyFeedback(Integer feedbackId, String replyContent, Integer adminId)
    }

    class CommentEntity {
        - Integer commentId
        - String cMsg
        - Integer status
        + Integer getCommentId()
    }

    class FeedbackEntity {
        - Integer feedbackId
        - String content
        - Integer status
        - String replyContent
        + Integer getFeedbackId()
    }

    interface CommentMapper {
        + Integer updateCommentStatus(Integer commentId, Integer status)
        + Integer deleteComment(Integer commentId)
    }

    interface FeedbackMapper {
        + Integer updateFeedbackReply(Integer feedbackId, String replyContent, Integer status)
    }

    interface MessageService {
        + Boolean sendMessage(MessageEntity message)
    }

    class PageResult<T> {
        - Long total
        - List<T> list
        + List<T> getList()
    }
```