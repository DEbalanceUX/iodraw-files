```mermaid
classDiagram
    %% 关系定义
    SystemParamServiceImpl ..|> SystemParamService : 实现
    BackupServiceImpl ..|> BackupService : 实现
    SystemMaintainController ..> SystemParamService : 依赖
    SystemMaintainController ..> BackupService : 依赖
    SystemMaintainController ..> LogService : 依赖
    SystemMaintainController ..> SystemParamDto : 依赖
    SystemMaintainController ..> DataBackupDto : 依赖
    SystemParamEntity "1" -- "1" SystemParamEntity : 一对一（paramKey-paramValue）
    BackupRecordEntity "n" -- "1" AdminEntity : 多对一

    %% 核心类定义
    class SystemParamDto {
        - String paramKey
        - String paramValue
        - String paramDesc
        + String getParamKey()
        + String getParamValue()
    }

    class DataBackupDto {
        - Integer backupType
        - String backupPath
        - String backupName
        + Integer getBackupType()
    }

    class SystemMaintainController {
        - SystemParamService systemParamService
        - BackupService backupService
        - LogService logService
        + Result updateSystemParam(SystemParamDto dto)
        + Result executeDataBackup(DataBackupDto dto)
        + List<SystemLogEntity> getSystemLog(Integer logType, Date[] timeRange)
    }

    interface SystemParamService {
        + String getParamByKey(String paramKey)
        + Boolean updateParam(SystemParamDto dto)
        + List<SystemParamEntity> getAllParams()
    }

    class SystemParamServiceImpl {
        - SystemParamMapper systemParamMapper
        + String getParamByKey(String paramKey)
        + Boolean updateParam(SystemParamDto dto)
    }

    interface BackupService {
        + Boolean executeBackup(Integer backupType, String path)
        + List<BackupRecordEntity> getBackupList()
        + Boolean restoreBackup(String backupName)
    }

    class BackupServiceImpl {
        - BackupMapper backupMapper
        + Boolean executeBackup(Integer backupType, String path)
    }

    class SystemParamEntity {
        - Integer paramId
        - String paramKey
        - String paramValue
        - String paramDesc
        + String getParamKey()
    }

    class BackupRecordEntity {
        - Integer recordId
        - String backupName
        - Integer backupType
        - String backupPath
        - Date backupTime
        - Integer adminId
        + Integer getRecordId()
    }

    class SystemLogEntity {
        - Integer logId
        - String logType
        - String logContent
        - Date logTime
        + Integer getLogId()
    }
```