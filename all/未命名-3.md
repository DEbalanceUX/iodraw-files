```mermaid
graph TD
    A(用户)

    subgraph 身份认证
        A --> A1(注册账号)
        A --> A2(登录系统)
    end

    subgraph 个人中心
        A --> B(查看/编辑个人资料)
        A --> C(修改登录密码)
    end

    subgraph 博客管理
        A --> D(撰写博客)
        D --> D1(保存草稿)
        D --> D2(设置文字格式)
        D --> D3(插入图片)
        A --> E(发布博客)
        A --> F(管理个人博客)
        F --> F1(编辑博客)
        F --> F2(删除博客)
        F --> F3(修改公开状态)
        A --> G(查看个人博客列表)
 
```