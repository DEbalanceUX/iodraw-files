```mermaid
classDiagram
    BokeAuditServiceImpl ..|> IBokeAuditService : implements
    BokeAuditController ..> IBokeAuditService : 依赖
    BokeAuditServiceImpl ..> IBokewenzhangMapper : 依赖
    IBokewenzhangMapper ..> BokewenzhangEntity : 关联

    class BokeAuditController {
        - IBokeAuditService bokeAuditService
        + Result auditBlog(BokeAuditDto dto)
        + PageResult getAuditBlogList(BokeAuditQueryDto dto)
        + Result getAuditLog(Integer blogId)
        
        class BokeAuditDto {
            - Integer blogId
            - Integer auditStatus
            - String auditOpinion
            - Integer auditorId
            + Integer getBlogId()
            + Integer getAuditStatus()
            + String getAuditOpinion()
            + Integer getAuditorId()
        }
        
        class BokeAuditQueryDto {
            - Integer auditStatus
            - String startTime
            - String endTime
            - Integer pageNum
            - Integer pageSize
            + Integer getAuditStatus()
            + String getStartTime()
            + String getEndTime()
            + Integer getPageNum()
            + Integer getPageSize()
        }
        
        class Result {
            - Integer code
            - String msg
            - Object data
            + static Result success(Object data)
            + static Result error(String msg)
            + static Result success()
            + Integer getCode()
            + String getMsg()
            + Object getData()
        }
        

            - Long total
            - Integer pages
            - List list
            - Integer pageNum
            - Integer pageSize
            + List getList()
            + Long getTotal()
            + Integer getPages()
            + Integer getPageNum()
            + Integer getPageSize()
        }
    }

    class IBokeAuditService {
        + Boolean auditBlog(Integer blogId, Integer auditStatus, String auditOpinion, Integer auditorId)
        + PageResult getAuditBlogList(Integer auditStatus, String startTime, String endTime, Integer pageNum, Integer pageSize)
        + List getAuditLog(Integer blogId)
    }

    class BokeAuditServiceImpl {
        - IBokewenzhangMapper bokewenzhangMapper
        + Boolean auditBlog(Integer blogId, Integer auditStatus, String auditOpinion, Integer auditorId)
        + PageResult getAuditBlogList(Integer auditStatus, String startTime, String endTime, Integer pageNum, Integer pageSize)
        + List getAuditLog(Integer blogId)
    }

    class IBokewenzhangMapper {
        + Integer updateAuditStatus(Integer blogId, Integer auditStatus, String auditOpinion, Integer auditorId, Date auditTime)
        + List selectByAuditCondition(Integer auditStatus, String startTime, String endTime, Integer start, Integer size)
        + Long countByAuditCondition(Integer auditStatus, String startTime, String endTime)
        + List selectAuditLog(Integer blogId)
        + BokewenzhangEntity selectById(Integer blogId)
    }

    class BokewenzhangEntity {
        - Integer blogId
        - String title
        - String content
        - Integer categoryId
        - Integer userId
        - Integer auditStatus
        - String auditOpinion
        - Integer auditorId
        - Date auditTime
        - Date createTime
        + Integer getBlogId()
        + String getTitle()
        + String getContent()
        + Integer getAuditStatus()
        + String getAuditOpinion()
        + void setAuditStatus(Integer auditStatus)
        + void setAuditOpinion(String auditOpinion)
        + void setAuditorId(Integer auditorId)
        + void setAuditTime(Date auditTime)
    }
```