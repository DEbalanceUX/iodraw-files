```mermaid
classDiagram
    FeedbackReplyServiceImpl ..|> IFeedbackReplyService : implements
    FeedbackReplyController ..> IFeedbackReplyService : 依赖
    FeedbackReplyServiceImpl ..> IMessagesMapper : 依赖
    IMessagesMapper ..> MessagesEntity : 关联

    class FeedbackReplyController {
        - IFeedbackReplyService feedbackReplyService
        + Result getFeedbackDetail(Integer messageId)
        + PageResult getFeedbackList(FeedbackQueryDto dto)
        + Result replyFeedback(FeedbackReplyDto dto)
        
        class FeedbackQueryDto {
            - Integer status
            - String startTime
            - String endTime
            - Integer pageNum
            - Integer pageSize
            + Integer getStatus()
            + String getStartTime()
            + String getEndTime()
            + Integer getPageNum()
            + Integer getPageSize()
        }
        
        class FeedbackReplyDto {
            - Integer messageId
            - String replyContent
            - Integer replyUserId
            + Integer getMessageId()
            + String getReplyContent()
            + Integer getReplyUserId()
        }
        

            - Integer code
            - String msg
            - Object data
            + static Result success(Object data)
            + static Result error(String msg)
            + static Result success()
            + Integer getCode()
            + String getMsg()
            + Object getData()
        
        

            - Long total
            - Integer pages
            - List list
            - Integer pageNum
            - Integer pageSize
            + List getList()
            + Long getTotal()
            + Integer getPages()
            + Integer getPageNum()
            + Integer getPageSize()

    }

    class IFeedbackReplyService {
        + MessagesEntity getFeedbackById(Integer messageId)
        + PageResult getFeedbackList(Integer status, String startTime, String endTime, Integer pageNum, Integer pageSize)
        + Boolean replyFeedback(Integer messageId, String replyContent, Integer replyUserId)
    }

    class FeedbackReplyServiceImpl {
        - IMessagesMapper messagesMapper
        + MessagesEntity getFeedbackById(Integer messageId)
        + PageResult getFeedbackList(Integer status, String startTime, String endTime, Integer pageNum, Integer pageSize)
        + Boolean replyFeedback(Integer messageId, String replyContent, Integer replyUserId)
    }

    class IMessagesMapper {
        + MessagesEntity selectById(Integer messageId)
        + List selectByFeedbackCondition(Integer status, String startTime, String endTime, Integer start, Integer size)
        + Long countByFeedbackCondition(Integer status, String startTime, String endTime)
        + Integer updateReplyContent(Integer messageId, String replyContent, Integer replyUserId, Date replyTime)
    }

    class MessagesEntity {
        - Integer messageId
        - Integer type
        - String content
        - String contact
        - Integer userId
        - Integer status
        - String replyContent
        - Integer replyUserId
        - Date replyTime
        - Date createTime
        + Integer getMessageId()
        + String getContent()
        + String getReplyContent()
        + Integer getStatus()
        + void setReplyContent(String replyContent)
        + void setReplyUserId(Integer replyUserId)
        + void setReplyTime(Date replyTime)
        + void setStatus(Integer status)
    }
```