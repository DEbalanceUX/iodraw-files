```mermaid
classDiagram
    %% 分层关系定义
    YonghuServiceImpl ..|> YonghuService : 实现
    YonghuController ..> YonghuService : 依赖
    YonghuServiceImpl ..> YonghuMapper : 依赖
    YonghuMapper ..> YonghuEntity : 关联

    %% Controller层（含入参DTO/出参Result）
    class YonghuController {
        - YonghuService yonghuService
        + Result updateUserInfo(YonghuUpdateDto dto, Integer userId)
        
        // 合并至Controller层的入参DTO
        class YonghuUpdateDto {
            - String nickname       // 新昵称
            - String avatar         // 新头像
            - String contact        // 新联系方式
            + String getNickname()
            + String getAvatar()
        }
        
        // 合并至Controller层的出参Result
        class Result {
            - Integer code
            - String msg
            - Object data
            + static Result success(Object data)
            + static Result error(String msg)
        }
    }

    %% Service层
    interface YonghuService {
        + Boolean updateUserInfo(Integer userId, String nickname, String avatar, String contact)
    }

    class YonghuServiceImpl {
        - YonghuMapper yonghuMapper
        + Boolean updateUserInfo(Integer userId, String nickname, String avatar, String contact)
    }

    %% Mapper层
    interface YonghuMapper {
        + Integer updateById(YonghuEntity entity)
        + YonghuEntity selectById(Integer userId)
    }

    %% Entity层
    class YonghuEntity {
        - Integer userId        // 用户ID
        - String nickname       // 昵称
        - String avatar         // 头像路径
        - String contact        // 联系方式
        - String username       // 用户名
        - String password       // 密码
        + Integer getUserId()
        + void setNickname(String nickname)
        + void setAvatar(String avatar)
    }
```