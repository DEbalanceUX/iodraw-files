```mermaid
classDiagram
    %% 分层关系定义
    YonghuServiceImpl ..|> IYonghuService : implements
    YonghuController ..> IYonghuService : 依赖
    YonghuServiceImpl ..> IYonghuMapper : 依赖
    IYonghuMapper ..> YonghuEntity : 关联

    %% Controller层（含入参DTO/出参Result）
    class YonghuController {
        - IYonghuService yonghuService
        
        /**
         * 修改用户个人信息
         * @param dto 个人信息修改参数
         * @param userId 用户ID
         * @return 操作结果
         */
        + Result updateUserInfo(YonghuUpdateDto dto, Integer userId)
        
        // 合并至Controller层的入参DTO
        class YonghuUpdateDto {
            - String nickname       // 新昵称
            - String avatar         // 新头像路径
            - String contact        // 新联系方式
            + String getNickname()
            + String getAvatar()
            + String getContact()
        }
        
        // 合并至Controller层的出参Result
        class Result {
            - Integer code          // 响应码（200成功/500失败）
            - String msg            // 响应信息
            - Object data           // 响应数据
            + static Result success(Object data)
            + static Result error(String msg)
        }
    }

    %% Service层 - 接口（规范命名：I+业务+Service）
    interface IYonghuService {
        /**
         * 执行个人信息修改
         * @param userId 用户ID
         * @param nickname 新昵称
         * @param avatar 新头像路径
         * @param contact 新联系方式
         * @return true=修改成功，false=修改失败
         */
        + Boolean updateUserInfo(Integer userId, String nickname, String avatar, String contact)
    }

    %% Service层 - 实现类
    class YonghuServiceImpl {
        - IYonghuMapper yonghuMapper
        
        /**
         * 实现个人信息修改逻辑
         * @param userId 用户ID
         * @param nickname 新昵称
         * @param avatar 新头像路径
         * @param contact 新联系方式
         * @return true=修改成功，false=修改失败
         */
        + Boolean updateUserInfo(Integer userId, String nickname, String avatar, String contact)
    }

    %% Mapper层 - 接口（规范命名：I+实体+Mapper）
    interface IYonghuMapper {
        
        + Integer updateById(YonghuEntity entity);
        
        /**
         * 根据ID查询用户
         * @param userId 用户ID
         * @return 用户实体
         */
        + YonghuEntity selectById(Integer userId);
    }

    %% Entity层
    class YonghuEntity {
        - Integer userId        // 主键ID
        - String nickname       // 昵称
        - String avatar         // 头像路径
        - String contact        // 联系方式
        - String username       // 登录账号
        - String password       // 登录密码（加密）
        // Getter/Setter
        + Integer getUserId()
        + void setNickname(String nickname)
        + void setAvatar(String avatar)
        + void setContact(String contact)
    }
```